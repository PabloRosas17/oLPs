import { ProjectsDirective } from './projects.directive';

describe('ProjectsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectsDirective();
    expect(directive).toBeTruthy();
  });
});
